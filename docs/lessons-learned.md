# Lessons Learned — Sarani

## Session 18 — 2026-04-07

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S18 | 2026-04-07 | problème | P0 | [BUG] Vidéo playback : 15+ itérations échouées (directUrl, 302 redirect, dual source, resolve=1). Root cause : CORS + Content-Disposition sur les download URLs SharePoint empêchent le browser de jouer la vidéo. | Streaming proxy via buffer (arrayBuffer → NextResponse). Le serveur fetch la vidéo côté serveur et la sert au browser. La seule approche qui fonctionne. | **RÈGLE : les vidéos SharePoint DOIVENT être streamées via un proxy serveur. Les download URLs directes ne fonctionnent PAS dans un `<video>` (CORS + Content-Disposition: attachment).** Contredit la règle S17 — mise à jour. | règle-globale | src/app/api/project-assets/[itemId]/route.ts, src/components/ui/VideoPlayer.tsx | fait | propagé |
| S18 | 2026-04-07 | problème | P0 | [BUG] Server Component crash : onClick/onError dans les children d'un Server Component crash le SSR. Deux crashs consécutifs (onError sur `<video>`, onClick sur `<a>` download). | Tous les event handlers retirés de la share page. VideoPlayer est un Client Component ("use client") qui gère les events. | **RÈGLE : ZÉRO event handler (onClick, onError, etc.) dans les Server Components Next.js — même dans les children de Client Components.** | règle-globale | CLAUDE.md | fait | propagé |
| S18 | 2026-04-07 | problème | P0 | [BUG] LinkedIn visual "Unsupported OpenType signature" : les fichiers Outfit-Bold.ttf étaient des pages HTML, pas des polices. Téléchargés via un lien de partage qui a retourné du HTML. | Remplacé par Poppins-Bold.ttf et Poppins-Regular.ttf téléchargés depuis Google Fonts (vrais TrueType). | **RÈGLE : toujours vérifier le type réel des fichiers binaires (fonts, images) avec `file` avant de les committer.** | documentation | public/fonts/ | fait | n/a |
| S18 | 2026-04-07 | problème | P0 | [BUG] Pipeline base64 key mismatch : le pipeline stockait le LinkedIn visual avec la clé "base64" mais l'endpoint le cherchait sous "base64Png" → régénération à chaque vue. | Aligné sur "base64Png" partout. | **RÈGLE : les clés de stockage DB doivent être identiques entre écriture et lecture. Tester le round-trip.** | documentation | src/app/api/admin/case-studies/ | fait | n/a |
| S18 | 2026-04-07 | insistance | P0 | [INSISTANCE x10] LinkedIn prompt : 15+ itérations de patches. Thomas rejetait chaque version. Le format pipe (X\|Y\|Z) revenait parce que les EXEMPLES dans le prompt l'utilisaient. | Réécrit from scratch : 7 vrais posts + pattern tagline/body/closer + 3 règles. 60 lignes au lieu de 120. | **RÈGLE : quand un prompt ne produit pas le bon output après 3 itérations, le réécrire de ZÉRO. Ne pas patcher — le LLM copie les exemples, pas les règles.** | règle-globale | CLAUDE.md | fait | à propager |
| S18 | 2026-04-07 | préférence fondateur | P0 | Thomas veut : tagline créative (2-6 mots) + body factuel + closer chaleureux. Le hook ne doit JAMAIS être un format pipe. Le 100/100 n'existe pas. Pas de bullet points, pas de jargon agence. Jouer avec le thème du projet (Summer = chaleur). | Prompt réécrit, 13 quality gates serveur (linkedin-gates.ts), scoreTotal retiré des inputs LLM. | Calibration LinkedIn S18 dans founder-preferences.md | founder-prefs | docs/founder-preferences.md | fait | propagé |
| S18 | 2026-04-07 | problème | P1 | [BUG] Tracker mobile 2min+ : cache DB froid après redéploiement. 80+ appels ClickUp API au cold start. | Cron `tracker-cache-warm` toutes les 25min + warm 10s après démarrage serveur. Stale-serve dans cache.ts. | **RÈGLE : les endpoints lourds (80+ appels API) DOIVENT avoir un cache warming au démarrage du serveur.** | documentation | src/lib/cron-scheduler.ts | fait | n/a |
| S18 | 2026-04-07 | problème | P1 | [BUG] Tracker ByteDance → TikTok : le merge Excel-first donnait les dates/statuts Excel au lieu de ClickUp. | Inversé l'ordre : ClickUp-only d'abord, Excel complète les gaps. | **RÈGLE : ClickUp est la source de vérité pour statuts/dates/contacts. Excel est la source de vérité pour les données financières.** | documentation | src/lib/integrations/tracker-merge.ts | fait | n/a |
| S18 | 2026-04-07 | problème | P1 | [BUG] Auto-select visuals ne trouvait jamais les images : resolveSharePointUrl essayait l'API /shares/ sur des webUrls (30s timeout × 3 variants = 90s). Les URLs AllItems.aspx avaient le path dans ?id=, pas dans le pathname. | Réécrit from scratch : skip sharing link API pour les webUrls, parser ?id= param, scanner les sous-dossiers les plus récents. | **RÈGLE : les URLs SharePoint AllItems.aspx ont le vrai chemin dans le paramètre `?id=`, PAS dans le pathname.** | documentation | src/lib/case-studies/auto-select-visuals.ts | fait | n/a |
| S18 | 2026-04-07 | problème | P1 | [BUG] ShareFolderModal naviguait vers "Others" malgré le bon listId dans ClickUp : le tracker passait un mauvais clickupListId, et le fallback API ne s'exécutait que si listId était vide (pas quand il était faux). | ClickUp API (getTask) est maintenant le step 1 — bypass le listId du tracker. | **RÈGLE : pour la navigation SP, toujours vérifier via l'API ClickUp directe. Ne jamais faire confiance au listId du tracker.** | documentation | src/components/admin/ShareFolderModal.tsx | fait | n/a |
| S18 | 2026-04-07 | problème | P1 | [BUG] Pipeline validation crashes : le LLM retournait des champs trop longs (metaDescription 200 chars), des stats en nombre variable, des catégories inventées, des slugs avec accents. | Auto-truncate + auto-fix programmatique pour slug, stats, category, subject. Schemas Zod relaxés (stats array au lieu de tuple, proofPoints default ""). | **RÈGLE : le pipeline case study ne doit JAMAIS crasher à cause d'un output LLM mal formé. Auto-fix en dernier recours.** | documentation | src/app/api/admin/case-studies/ | fait | n/a |
| S18 | 2026-04-07 | problème | P2 | [BUG] Case studies publiés n'apparaissaient pas sur /work : la page /work utilisait des données statiques, pas la DB. La page /case-studies existait mais Thomas ne l'utilise pas. | /work et /work/[slug] requêtent maintenant la DB pour les case studies publiés. force-dynamic. | **RÈGLE : demander à Thomas QUELLE PAGE affiche le contenu avant de coder la publication.** | documentation | src/app/work/ | fait | n/a |
| S18 | 2026-04-07 | problème | P2 | [BUG] Toutes les QA (3 audits + 1 exhaustif) n'ont pas détecté les crashs Server Component (onClick/onError). | Ajouté 188 tests anti-régression (anti-regression-s18.test.ts) qui vérifient le code source par grep. | **RÈGLE : les tests anti-régression par analyse statique du code source (grep) détectent des classes de bugs que les tests unitaires classiques ne couvrent pas.** | documentation | tests/unit/anti-regression-s18.test.ts | fait | n/a |

## Session 17 — 2026-04-04

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S17 | 2026-04-04 | insistance | P0 | [INSISTANCE FONDATEUR x3] 15+ commits sur les mêmes 3 bugs (TikTok mapping, vidéo, pipeline). Chaque "fix" était testé en statique mais jamais en live. Thomas a dit "c'est déprimant" et "j'en ai marre de tester pour l'équipe". | Invoqué @moi pour prendre le lead. Approche changée : investigation root cause au lieu de patchs symptomatiques. | **RÈGLE RENFORCÉE : quand un bug revient 3+ fois, STOP les patchs. Lancer un agent d'investigation dédié pour trouver la root cause. Ne plus patcher les symptômes.** | règle-globale | CLAUDE.md | fait | propagé (règle 24 déjà en place) |
| S17 | 2026-04-04 | problème | P0 | [ROOT CAUSE] TikTok SP mapping : le ShareFolderModal fetchait le lien SP depuis les custom fields ClickUp AVANT de tenter le config lookup. Le lien SP générique (dossier parent) bypassait le config lookup déterministe. Le config lookup n'était JAMAIS atteint. | Réordonné : si config mapping existe (clickupListId ou clickupListName), skip le fetch ClickUp SP link et utilise loadClientRoot directement. Ajouté endpoint /task-list pour fetch le listId quand il manque. | **RÈGLE : le config lookup déterministe (ID → path) doit TOUJOURS être prioritaire sur les liens dynamiques (custom fields ClickUp). Les liens dynamiques sont des fallbacks, pas des primaires.** | agent-spécifique | src/components/admin/ShareFolderModal.tsx | fait | n/a |
| S17 | 2026-04-04 | problème | P0 | [BUG] Pipeline case study timeout 15s pour des appels LLM qui prennent 20-45s. Le step 1 échouait systématiquement avec "Request timed out". | Tous les timeouts pipeline passés à 60s. Audit @ia des 40+ routes LLM : 4 routes corrigées (seo 120s, presentation 120s, rerun 120s, classify 20s). | **RÈGLE : un appel LLM avec maxTokens > 2048 doit avoir un timeout >= 60s. Avec maxTokens > 8192, timeout >= 120s.** | règle-globale | CLAUDE.md | fait | propagé |
| S17 | 2026-04-04 | problème | P0 | [BUG] Vidéo playback : le proxy streaming ne fonctionnait pas en production (5 tentatives précédentes). Root cause : proxyer des vidéos via API routes Next.js sur Replit est structurellement non fiable (timeout, double bande passante, worker killed). | Remplacement du proxy par les URLs directes SharePoint (@microsoft.graph.downloadUrl) qui sont pré-authentifiées et expirent après ~1h. ISR revalide les URLs avant expiration. | **RÈGLE : ne JAMAIS proxyer des fichiers vidéo via les API routes Next.js. Utiliser les @microsoft.graph.downloadUrl directement. Elles sont anonymes et n'ont pas besoin de CORS.** | agent-spécifique | src/app/project/[clientSlug]/[projectSlug]/page.tsx | fait | n/a |
| S17 | 2026-04-04 | problème | P1 | [BUG] Publish case study : le POST et DELETE dépendaient de writeFileSync sur case-studies.ts. En production Replit, le filesystem est éphémère et l'écriture peut échouer → publish bloqué. | Rendu le file write best-effort (try/catch). DB est la source de vérité. Ajouté revalidatePath pour refresh instantané. | **RÈGLE : la DB est TOUJOURS la source de vérité pour le contenu dynamique. L'écriture fichier statique est un bonus, jamais un bloquant.** | documentation | docs/lessons-learned.md | fait | n/a |
| S17 | 2026-04-04 | problème | P1 | [BUG] Extract-branding "No clients to process" : les brandGuidelinesLink n'étaient pas en DB car sync-branding n'avait pas été appelé après les enrichissements. Puis les PDFs étaient rejetés (limite 5MB → relevée à 30MB → puis Range download pour les >20MB). | Extract-branding écrit lui-même les liens SP en DB (FALLBACK_GUIDELINES hardcodé). Range download des premiers 20MB pour les gros PDFs. | **RÈGLE : un endpoint d'extraction ne doit JAMAIS dépendre d'un autre endpoint pour ses données d'entrée. Il doit être self-contained.** | documentation | docs/lessons-learned.md | fait | n/a |
| S17 | 2026-04-04 | problème | P1 | [BUG] IDs ClickUp dupliqués dans config.ts : 3 paires (Shop SEA/UK, Others/CCA, Shop EMEA/TTS Global). getSubdivisionByListId retournait le PREMIER match → mauvais dossier SP. | Thomas a fourni les vrais IDs. Corrigé les 3 paires. Ajouté vérification de doublons dans le pipeline de test. | **RÈGLE : vérifier systématiquement les doublons dans les mappings de configuration. Un script de validation devrait tourner en CI.** | documentation | docs/lessons-learned.md | fait | n/a |
| S17 | 2026-04-04 | préférence fondateur | P1 | Thomas veut un ton LinkedIn "sûr de nos forces mais humble" — laisser les résultats parler. Pas de CTA commercial systématique (7 types de fermeture). Pas de hashtags. Titre visuel = 2-4 mots style poster. Traits d'esprit bienvenus. | Prompt social réécrit avec 7 closings, ton calibré, visualTitle 2-4 mots, hashtags supprimés. | Calibration créative LinkedIn documentée dans le prompt social et dans founder-preferences. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| S17 | 2026-04-04 | pattern | P1 | Le pattern "5 reviews en parallèle" (QA + UX + Design + IA + Persona) puis corrections groupées est très efficace. 12 bugs détectés en une passe, tous corrigés en un lot. Plus efficace que les reviews séquentielles. | — | Utiliser ce pattern pour toute feature majeure : lancer 4-5 reviews en parallèle, synthétiser, corriger en lot. | documentation | docs/lessons-learned.md | fait | n/a |
| S17 | 2026-04-04 | pattern | P2 | Satori (next/og) pour les visuels LinkedIn = 0$ cost, <500ms, pixel-perfect. DALL-E écarté car impossible d'injecter des logos fidèles et de contrôler un layout précis. Choix validé par Thomas et @ia. | — | **RÈGLE : pour les visuels template-based (logos, layout fixe, branding strict), Satori/next-og est toujours supérieur à la génération IA.** DALL-E/Midjourney uniquement pour les visuels créatifs sans contrainte de layout. | documentation | docs/lessons-learned.md | fait | n/a |
| S17 | 2026-04-04 | performance-ia | P2 | Tracker mobile très lent (6-18s en cold cache, 100-200 appels API externes). Cache TTL de 10min trop court. ShareFolderModal (888 lignes) chargé statiquement. | Lazy-load ShareFolderModal, cache TTL x3 (30min), Cache-Control navigateur 5min. | Pour les pages avec beaucoup d'appels API externes, les TTL de cache doivent être calibrés sur la fréquence réelle de changement des données, pas sur un défaut arbitraire. | documentation | docs/lessons-learned.md | fait | n/a |

## Session 16 — 2026-04-03

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S16 | 2026-04-03 | problème | P0 | [BUG] claude-sonnet-4-latest n'est PAS un alias Anthropic valide — retourne 404. S15 l'avait changé depuis claude-sonnet-4-6-latest pensant que c'était un alias auto-update. En réalité, le suffixe -latest ne fonctionne QUE dans une famille mineure (4-6), pas au niveau majeur (4). | Revert à claude-sonnet-4-6-latest dans src/lib/ai/claude.ts. | **CORRECTION de la règle S15 : utiliser claude-sonnet-4-6-latest (pas claude-sonnet-4-latest).** Le suffixe -latest ne fonctionne que dans une famille mineure. Vérifier toujours la validité d'un alias modèle avant de le déployer. | règle-globale | src/lib/ai/claude.ts, docs/founder-preferences.md | fait | propagé |
| S16 | 2026-04-03 | insistance | P0 | [INSISTANCE FONDATEUR x2] Thomas a signalé que LES 4 BUGS de S15 ne fonctionnaient pas en production. Les "fixes" n'avaient pas été testés en live. "c'est incroyable sur aucun de mes sujets ca ne marche" | Tests écrits avec données réelles (noms ClickUp + dossiers SP exacts), tsc vérifié, 594 tests. Mais TOUJOURS pas de test live (pas de .env). | **RÈGLE RENFORCÉE : ne JAMAIS confirmer un fix sans test live. Si le test live est impossible (pas d'env), le dire EXPLICITEMENT à Thomas** — "fix appliqué, tests unitaires OK, MAIS non testé en live". | règle-globale | CLAUDE.md | fait | propagé |
| S16 | 2026-04-03 | problème | P0 | [REFONTE] Le fuzzy matching de noms de dossiers SP était fondamentalement la mauvaise approche. Thomas a pointé : le ClickUp list ID est déjà connu, il suffit de le mapper à un dossier SP via la config. Le fuzzy matching échouait sur les caractères spéciaux (P&SEA ≠ P&E SEA), le scoring (2/8 < seuil), les séparateurs (em-dash). | Refonte : getSubdivisionByListId() lookup direct dans config.ts. Subdivisions ajoutées pour 9 clients (TikTok 21, Sony 3, Ubi 7, Aujan 3, PICO 2, Aristocrat 2, Bose, Lamarck, CMC). | **RÈGLE : quand un identifiant unique existe (ClickUp list ID), TOUJOURS l'utiliser pour le mapping au lieu de fuzzy-matcher des noms.** Le fuzzy matching est un pis-aller, pas une solution. Plus généralement : avant de coder un algorithme de matching, vérifier si un mapping déterministe existe déjà dans la config ou les business rules. | règle-globale | CLAUDE.md, src/lib/integrations/config.ts | fait | propagé |
| S16 | 2026-04-03 | préférence fondateur | P1 | [CORRECTION PRÉFÉRENCE] La préférence S15 "modèle auto-update alias" était basée sur une prémisse fausse (claude-sonnet-4-latest valide). Le bon alias est claude-sonnet-4-6-latest. Thomas veut quand même un alias stable — juste le bon. | Corrigé dans docs/founder-preferences.md. | Le bon alias Anthropic est claude-sonnet-4-6-latest. Mettre à jour quand la famille 4.7 sortira. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| S16 | 2026-04-03 | pattern | P1 | Le pattern "utilise le mapping existant" est plus robuste que "améliore l'algorithme de matching". Thomas a dit "pourquoi tu pars du nom du projet ??? pars simplement de son espace click-up". La bonne question avant tout algorithme : "est-ce que les données d'entrée contiennent déjà un identifiant de mapping ?" | — | Avant d'écrire du fuzzy matching, vérifier : (1) existe-t-il un ID unique dans les données ? (2) existe-t-il déjà un mapping dans la config/business rules ? (3) seulement si non → fuzzy matching en fallback. | documentation | docs/lessons-learned.md | fait | n/a |

## Session 15 — 2026-04-03

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S15 | 2026-04-03 | problème | P0 | [BUG CRITIQUE — ROOT CAUSE] Video playback cassé depuis 4 itérations. Les 4 premières tentatives ciblaient les symptômes (CSP media-src, CORS headers Access-Control-Allow-Origin, crossOrigin attribute, source tag). La vraie cause : SharePoint retourne `application/octet-stream` comme mimeType pour les vidéos uploadées (pas video/mp4). Le proxy voyait un MIME non-vidéo → 302 redirect vers SharePoint → cross-origin sans CORS → playback cassé. | Ajout VIDEO_EXT_TO_MIME mapping dans le proxy route.ts ET dans la share page page.tsx. Détection par extension (.mp4/.mov/.webm/etc.) quand le mimeType de SharePoint est incorrect. | **RÈGLE : ne JAMAIS faire confiance au mimeType de SharePoint pour les fichiers uploadés.** SharePoint retourne souvent `application/octet-stream`. Toujours avoir un fallback par extension de fichier. Plus généralement : quand un bug persiste après 3+ tentatives de fix, arrêter de traiter les symptômes et chercher la cause racine en remontant la chaîne de données (d'où vient le mimeType ? → Graph API → SharePoint → octet-stream). | règle-globale | project-context.md (section SharePoint) | fait | propagé |
| S15 | 2026-04-03 | problème | P1 | [BUG] Case study generation 404 : le modèle `claude-sonnet-4-5-20241022` n'existe plus dans l'API Anthropic. Le DEFAULT_MODEL dans claude.ts était un nom de version daté qui est devenu invalide. | Changé DEFAULT_MODEL en `claude-sonnet-4-latest` — alias auto-update qui suit les versions mineures Sonnet 4.x automatiquement. | **RÈGLE : utiliser les alias `-latest` pour les modèles Claude dans les appels API.** `claude-sonnet-4-latest` au lieu de `claude-sonnet-4-6-latest` ou d'un ID daté. Évite les 404 quand Anthropic retire/renomme des versions. | agent-spécifique | src/lib/ai/claude.ts | fait | n/a |
| S15 | 2026-04-03 | problème | P1 | [BUG] Case study generation 409 race condition : le bouton "Create Case Study" pour les candidates appelait d'abord PATCH (set status=generating) puis POST (generate). Le PATCH échouait en 409 si le statut était déjà "generated". Pour les candidates (source="candidate"), le PATCH n'est pas nécessaire — POST suffit. | handleStarOverride appelle POST directement pour les candidates, sans PATCH préalable. Le paramètre `source` est maintenant passé au handler. | Quand un workflow a 2 appels séquentiels (PATCH puis POST), vérifier si les 2 sont nécessaires pour tous les cas. Les race conditions PATCH→POST sont un pattern récurrent. | agent-spécifique | src/app/admin/(authenticated)/closures/page.tsx | fait | n/a |
| S15 | 2026-04-03 | problème | P1 | [BUG] Comment badges affichés sur 4 assets au lieu d'1 : les commentaires utilisaient `fileName` comme clé, mais 4 assets dans des dossiers différents avaient le même nom. | Clé de commentaire changée en `itemId` (ID SharePoint unique) au lieu de `fileName`. | **RÈGLE : ne JAMAIS utiliser le nom de fichier comme identifiant unique pour les assets SharePoint.** Utiliser `item.id` (Graph API ID). Les noms de fichiers sont dupliqués entre dossiers. | agent-spécifique | src/app/project/[clientSlug]/[projectSlug]/page.tsx | fait | n/a |
| S15 | 2026-04-03 | problème | P1 | [BUG] SharePoint folders ont des préfixes numériques ("15. TikTok P&E SEA") et un sous-dossier intermédiaire "Projects" que le matching ne prenait pas en compte. | stripNumberPrefix() + auto-navigation dans le sous-dossier "Projects" avant le matching par nom. | Les dossiers SharePoint d'entreprise ont souvent des conventions de nommage (préfixes numériques, sous-dossiers standards). Le matching DOIT normaliser les noms avant comparaison. | agent-spécifique | src/components/admin/ShareFolderModal.tsx | fait | n/a |
| S15 | 2026-04-03 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas préfère un alias de modèle auto-update (`claude-sonnet-4-latest`) plutôt qu'une version datée, pour éviter les 404 futurs. Il accepte le risque de changement de comportement mineur en échange de la résilience. | Changé DEFAULT_MODEL en `claude-sonnet-4-latest`. | Les modèles LLM doivent utiliser des alias stables (-latest) sauf si un comportement spécifique d'une version est critique. Thomas préfère la résilience à la reproductibilité exacte. | founder-prefs | docs/founder-preferences.md | fait | propagé |
| S15 | 2026-04-03 | pattern | P1 | Le pattern "QA deep-dive ciblé" (lancer un agent QA uniquement sur un bug persistant avec instruction de chercher la root cause) est très efficace. Le QA a trouvé la root cause vidéo en remontant la chaîne de données alors que 4 fixes précédents échouaient. | — | Pour les bugs persistants (3+ tentatives), lancer un agent QA dédié avec la consigne explicite "cherche la cause racine, pas les symptômes". L'agent doit remonter la chaîne complète de données. | documentation | docs/lessons-learned.md | fait | n/a |
| S15 | 2026-04-03 | pattern | P2 | cleanEmailSubject() est un bon exemple de fix "upstream" : au lieu de corriger le matching, on nettoie les données en entrée. Les préfixes email (Re:/Fwd:/Tr:) polluaient la similarité de sujet. | — | Quand le matching échoue, vérifier d'abord si les données d'entrée sont propres. Le nettoyage upstream est souvent plus robuste que l'ajustement de l'algorithme de matching. | aucune | — | fait | n/a |

## Session 13 — 2026-04-03

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S13 | 2026-04-03 | insistance | P0 | [INSISTANCE FONDATEUR] Les QA font des revues de code statique mais NE TESTENT PAS le workflow réel. Thomas a demandé "qui a testé ?" et la réponse honnête était "personne n'a appelé les APIs ni ouvert les pages en live". Le QA vérifie les imports, les types, la logique ligne par ligne — mais pas ce que voit l'utilisateur final. | Documenté honnêtement que le test live ne peut se faire que sur Replit (pas de .env dans l'env Claude Code). | **RÈGLE : quand un QA dit "PASS", il DOIT préciser s'il a fait un test statique (code review) ou un test live (appel API/rendu). Ne JAMAIS laisser entendre qu'un test live a été fait quand c'est juste du code review.** | règle-globale | CLAUDE.md, .claude/agents/qa.md | fait | propagé |
| S13 | 2026-04-03 | insistance | P0 | [INSISTANCE FONDATEUR] Ne JAMAIS renommer/modifier un label ou un texte visible sans que Thomas l'ait explicitement demandé. L'orchestrateur a renommé "Creative Proposal" en "Deliverables" alors que Thomas n'avait rien demandé — il a dû corriger. | Revert immédiat du renommage. | **RÈGLE : ne JAMAIS changer un label, un texte client-facing, un nom de section, un CTA sans demande explicite de Thomas. Les modifications visuelles/textuelles non sollicitées sont interdites.** | règle-globale | CLAUDE.md | fait | propagé |
| S13 | 2026-04-03 | problème | P0 | [BUG CRITIQUE] Les URLs SharePoint `@microsoft.graph.downloadUrl` expirent après ~1h. Les pages de présentation affichaient des images cassées pour tout client qui ouvrait le lien le lendemain. Détecté par @arya (pas par @qa). | Créé un endpoint proxy `/api/project-assets/[itemId]` qui re-fetch le download URL frais et redirige (302, cache 5min). | Les URLs de téléchargement Graph API sont TEMPORAIRES. Ne JAMAIS les stocker ou les servir directement dans le HTML. Toujours passer par un proxy qui re-fetch au runtime. | agent-spécifique | src/app/project/[clientSlug]/[projectSlug]/page.tsx | fait | n/a |
| S13 | 2026-04-03 | problème | P1 | [BUG] Le filtre client du tracker affichait toutes les entités/divisions (TikTok France, Sony Professional...) au lieu des clients principaux. Et les petits clients ("Other customers" ClickUp) encombraient la liste. | Filtre basé sur CLIENT_MAPPINGS (clients principaux) + "Others" pour le reste. | Les filtres client doivent afficher les entités de haut niveau, pas les divisions. Utiliser les CLIENT_MAPPINGS comme source de vérité. | agent-spécifique | src/app/admin/(authenticated)/tracker/page.tsx | fait | n/a |
| S13 | 2026-04-03 | problème | P1 | [BUG] Le Share modal affichait le mauvais dossier SP (dossier racine client au lieu du dossier projet) parce que le lien SP n'était pas extrait des custom fields ClickUp. Les custom fields de type "url" retournent parfois un objet `{ url: "..." }` au lieu d'une string. | extractSharePointLink scanne string + object + stringify fallback. | Les custom fields ClickUp peuvent retourner des types variés (string, { url }, { value }, number). Toujours vérifier les 3 formats. | agent-spécifique | src/lib/integrations/tracker-merge.ts | fait | n/a |
| S13 | 2026-04-03 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut que les filtres du tracker survivent au retour arrière du navigateur. | Filtres stockés dans les URL search params via replaceState. | Les filtres de toute page de listing doivent être persistés dans l'URL (pas juste en state React). | agent-spécifique | docs/lessons-learned.md | fait | n/a |
| S13 | 2026-04-03 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut un pipeline case study multi-agents (creative-strategy → copywriter → social) avec visuels proposés par les agents + édition PM + publication séparée par canal + auto-post LinkedIn. | Spécifié dans le mémo de reprise pour session 14. | Les pipelines de contenu doivent impliquer les agents spécialisés en chaîne, pas un prompt unique. | documentation | docs/product/presentation-features-specs.md | à-faire | n/a |

| S12 | 2026-04-02 | insistance | P0 | [INSISTANCE FONDATEUR] Les QA vérifient la structure (imports, types, tsc) mais PAS la logique métier réelle. Thomas a demandé 5+ QA cette session — aucun n'a détecté que les reviews TikTok allaient dans "Others" au lieu de "TikTok", que les emails Sarani passaient le filtre, ou que le feedback copiait-collait le body. Les tests unitaires mockent des données parfaites au lieu de tester les vrais payloads. | Ajout de tests avec payloads réels (review TikTok, email Sony thread, feedback Lamarck). | **RÈGLE : chaque QA DOIT inclure des scénarios end-to-end avec des données réelles** (pas mockées). Simuler les vrais payloads JSON de l'inbox, les vrais summaries des review items, les vrais emails avec threads. Un QA qui dit "PASS" sans avoir testé un seul cas réel est un QA qui ne sert à rien. | règle-globale | CLAUDE.md, .claude/agents/qa.md | fait | propagé |

## Session 12 — 2026-04-01

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S12 | 2026-04-01 | insistance | P0 | [INSISTANCE FONDATEUR] Les tests QA doivent GRANDIR AUTOMATIQUEMENT au fur et à mesure des retours de Thomas et de l'avancement du projet. Chaque bug signalé par Thomas, chaque commentaire, chaque fix doit générer un test unitaire correspondant pour éviter la régression. On ne teste pas "une fois" — les tests sont un patrimoine vivant qui s'enrichit à chaque session. Thomas passe trop de temps à tester manuellement des choses logiques. | Création de tests unitaires inbox (classifier, brief-extractor, feedback-extractor, matchScore, filterItems, sarani-filter). Refactoring pour exporter les fonctions testables. | **RÈGLE : les tests QA sont incrémentaux et automatiques.** À chaque bug signalé par Thomas, chaque fix appliqué, chaque nouvelle feature : @qa DOIT ajouter un test correspondant AVANT de déclarer le travail terminé. Les tests ne sont jamais "complets" — ils grandissent avec le projet. @fullstack ne peut pas déclarer un fix "done" sans un test qui le couvre. @qa audite en vérifiant que le nombre de tests a augmenté proportionnellement aux changements. | règle-globale | CLAUDE.md (règles communes), .claude/agents/qa.md, .claude/agents/fullstack.md | fait | propagé |
| S12 | 2026-04-01 | insistance | P0 | [INSISTANCE FONDATEUR] Le CreateBriefModal copiait-collait l'email brut au lieu d'appeler le LLM brief-extractor. Thomas a vu un brief = copie email et a demandé "où est la valeur ajoutée d'Arya ?". Le prompt LLM était bon mais jamais appelé depuis le modal — seulement depuis le cron auto-brief. | Ajout d'un endpoint /api/admin/brief/extract + appel LLM au mount du CreateBriefModal. | **RÈGLE : tout composant qui affiche du contenu généré par IA DOIT appeler le LLM au moment de l'affichage, pas se contenter de données pré-remplies manuellement.** Si le champ est vide (email classifié avant le fix), appeler le LLM à la volée. Ne jamais montrer un placeholder vide ou un copier-coller brut quand un prompt LLM existe pour ce cas. | règle-globale | .claude/agents/fullstack.md | fait | propagé |
| S12 | 2026-04-01 | problème | P0 | [BUG CRITIQUE] Le cron-scheduler.ts référençait BASE_URL qui n'existait pas (variable renommée en INTERNAL_URL). Résultat : AUCUN cron ne tournait (poll-emails, scan-knowledge, renew-subscriptions). Les nouveaux emails n'étaient pas classifiés, le knowledge learning ne fonctionnait pas, les draftReply étaient vides. | Fix: BASE_URL → INTERNAL_URL dans la guard de runJob(). | Les agents doivent vérifier que les variables utilisées dans les guards existent réellement. Un guard `if (!VARIABLE)` qui référence une variable undefined retourne toujours true et désactive silencieusement la feature. | agent-spécifique | src/lib/cron-scheduler.ts | fait | n/a |
| S12 | 2026-04-01 | insistance | P1 | [INSISTANCE FONDATEUR] Les emails internes Sarani (réponses de l'équipe dans les threads) apparaissaient dans l'inbox. Le from était le client (Sony) mais le body contenait la réponse de Fanny@sarani.studio. Le filtre from ne suffisait pas. | Triple filtre : Graph API (from), cron (body detection @sarani.studio + "De :"), client-side (retroactif). | **RÈGLE : le filtrage des emails internes doit vérifier le FROM et le BODY.** Un thread email où un membre Sarani a répondu a le from = client mais le body commence par la réponse Sarani. Détecter @sarani.studio dans les 400 premiers chars + marqueurs de thread ("De :", "From:", "Envoyé :"). | agent-spécifique | src/lib/inbox/sarani-filter.ts, src/lib/integrations/email.ts | fait | n/a |
| S12 | 2026-04-01 | insistance | P1 | [INSISTANCE FONDATEUR] Le feedback extractor disait "No specific changes detailed" quand un client partageait un lien SharePoint vers un dossier de feedback. Or le client partage des fichiers annotés — les changements sont DANS les fichiers. | Règle ajoutée au prompt : "SharePoint/Drive link = annotations inside files. Download and apply all markup." + "ARYA NOTES" en haut du feedback au lieu de "QUESTIONS FOR PM" en bas. | Les agents IA doivent comprendre que les liens cloud (SharePoint, Drive, Dropbox) vers des dossiers "Feedback" contiennent des fichiers annotés. Ce n'est pas "aucun changement spécifié" — c'est "télécharge et applique les annotations". | agent-spécifique | src/lib/ai/prompts/feedback-extractor.ts | fait | n/a |
| S12 | 2026-04-01 | insistance | P0 | [INSISTANCE FONDATEUR] 7+ erreurs de build TypeScript consécutives. Thomas a redéployé 7 fois pour rien. Cause racine : les agents commitent sans lancer `tsc --noEmit`. Les tests vitest passent mais ne vérifient pas les types cross-fichiers. Chaque ajout de type (InboxItemType, InboxItemFilterable) cassait 3-4 fichiers non vérifiés. | Ajout systématique de `npx tsc --noEmit` avant chaque commit. | **RÈGLE ABSOLUE : lancer `npx tsc --noEmit` AVANT chaque commit.** Si tsc retourne des erreurs → corriger → relancer → 0 erreurs → ALORS commiter. Les tests vitest ne suffisent PAS. Un commit qui casse le build = un commit qui n'aurait jamais dû être pushé. 7 erreurs consécutives = 30+ min perdues par Thomas. | règle-globale | CLAUDE.md (règle 22), .claude/agents/fullstack.md (règle 5) | fait | propagé |

## Session 11 — 2026-04-01

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S11 | 2026-04-01 | insistance | P0 | [INSISTANCE FONDATEUR] Les reviews vérifient la qualité du code mais PAS le workflow réel. Les 3 reviewers (arya 8.5/10, ux 7.5/10, qa 7/10) ont vu que les boutons existaient, étaient bien labelisés, avaient les bons tokens — mais AUCUN n'a testé "je clique → qu'est-ce qui se passe concrètement pour la PM ?". Résultat : tous les boutons faisaient "Done — check ClickUp" sans aucun modal de validation ni brief à revoir. Le workflow FONDAMENTAL (email → voir le brief → valider → créer projet) n'existait pas malgré 3 reviews PASS. Cause racine : les agents évaluent le code, pas l'expérience utilisateur bout-en-bout. | Implémentation de modals de validation pour chaque action. | **RÈGLE : toute review DOIT inclure un test de workflow bout-en-bout.** Le reviewer simule le parcours complet : "je suis la PM, un email arrive, je clique sur Create Brief → qu'est-ce que je VOIS ?" Si la réponse est "un toast et l'item disparaît", c'est un FAIL même si le code est propre. Le workflow prime sur le code. Ajouter une section "Workflow walkthrough" obligatoire dans chaque review. | règle-globale | CLAUDE.md, .claude/agents/reviewer.md, .claude/agents/ux.md, .claude/agents/client-manager.md | fait | propagé |
| S11 | 2026-04-01 | insistance | P0 | [INSISTANCE FONDATEUR] Les reviews doivent être COMPLÈTES, pas partielles. Quand Thomas demande une review @arya/@ux/@design, il attend une revue de TOUS les écrans et TOUS les cas d'usage — pas seulement la feature qui vient d'être ajoutée. L'inbox affichait du JSON brut pour les emails (illisible pour une PM) et AUCUN reviewer ne l'a signalé en 5 reviews successives (arya 8/10, ux 9/10, design 8.5/10). Le JSON brut était visible depuis la S10 — 1 session entière sans détection. C'est un échec de process majeur : les reviewers évaluaient les nouvelles features sans vérifier l'état réel de l'écran complet. | Création d'EmailCard.tsx pour afficher proprement les emails (expéditeur, sujet, body, actions contextuelles). Remplacement du InboxCard générique par un composant dédié. | **RÈGLE ABSOLUE : toute review demandée par Thomas est une review COMPLÈTE de l'écran/page, pas seulement de la feature ajoutée.** Le reviewer DOIT : (1) lire TOUS les composants affichés sur la page, (2) simuler TOUS les types de données possibles (email, brief, quote, alert, noise), (3) vérifier que chaque type d'item a un rendu adapté et lisible, (4) signaler tout contenu brut/JSON/technique visible par l'utilisateur final. Un score de 8+/10 avec du JSON brut visible = review défaillante. | règle-globale | CLAUDE.md (règles communes agents), .claude/agents/reviewer.md, .claude/agents/ux.md, .claude/agents/design.md, .claude/agents/client-manager.md | fait | propagé |
| S11 | 2026-04-01 | insistance | P0 | [INSISTANCE FONDATEUR] 3 erreurs de build consécutives sur Replit. Les agents @fullstack doivent vérifier le build (tsc --noEmit ou next build) AVANT de déclarer le travail terminé. L'environnement Claude Code n'a pas node_modules → le tsc local ne détecte pas les erreurs réelles. Les agents doivent au minimum vérifier : (1) les imports existent, (2) les signatures de fonctions sont correctes (ex: z.record en Zod v4 prend 2 args), (3) les types explicites sur les variables inférées circulairement. | Fixes appliqués : type annotation explicite sur graphFetch, z.record(z.string(), z.unknown()) | **RÈGLE : vérification build obligatoire.** Tout agent @fullstack DOIT grep les patterns d'erreur connus (z.record à 1 arg, graphFetch sans type, imports manquants) avant de déclarer son travail terminé. Si node_modules n'est pas installé localement, au minimum vérifier la cohérence des imports et des signatures. | règle-globale | .claude/agents/fullstack.md | fait | propagé |
| S11 | 2026-04-01 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas ne veut AUCUN service externe pour les crons (pas de UptimeRobot, cron-job.org, etc.). Tout doit être géré en interne avec Replit + Claude. Solution : cron scheduler interne via Next.js instrumentation.ts + setInterval. | Créé src/lib/cron-scheduler.ts + src/instrumentation.ts — les crons tournent automatiquement au démarrage du serveur | **RÈGLE : zéro dépendance externe pour l'infrastructure.** Ne JAMAIS recommander de service tiers (UptimeRobot, cron-job.org, etc.) pour des tâches récurrentes. Implémenter un scheduler interne (setInterval, instrumentation.ts) ou un mécanisme Replit natif. Thomas veut une autonomie complète. | règle-globale | CLAUDE.md, .claude/agents/infrastructure.md, .claude/agents/fullstack.md | fait | propagé |
| S11 | 2026-04-01 | problème | P1 | [BUG] CacheSource type union n'inclut pas "arya" → build fail sur auto-quote route qui utilise logSync({ source: "arya" }). Chaque nouveau source doit être ajouté au type union dans cache.ts. | Ajouté "arya" au type CacheSource | Les agents doivent vérifier les types unions quand ils utilisent une valeur littérale dans une fonction typée. Grep le type avant d'utiliser une nouvelle valeur. | agent-spécifique | src/lib/integrations/cache.ts | fait | n/a | L'orchestrateur a présenté des exemples fictifs de knowledge entries (faux noms de contacts, fausses préférences clients) quand Thomas a demandé des exemples du scan email. Au lieu de dire "le scan n'a pas tourné, je n'ai aucune donnée réelle", il a inventé des données réalistes — violation directe de la règle absolue n°2 "Zéro invention de données". Thomas a rappelé : "Règle d'or absolue : ne jamais inventer d'information." | Aucune donnée fictive ne doit être présentée comme exemple, même à titre illustratif. Si une donnée n'existe pas, dire "je n'ai pas cette information" et proposer de lancer le scan pour obtenir les vraies données. | **RENFORCEMENT RÈGLE N°2 : quand un utilisateur demande des exemples d'une feature qui n'a pas encore tourné en production, NE JAMAIS fabriquer de faux exemples réalistes. Répondre : "Le [système] n'a pas encore été exécuté, il n'y a aucune donnée réelle. Voulez-vous le lancer maintenant ?" Si des exemples illustratifs sont absolument nécessaires, les marquer EXPLICITEMENT comme "[EXEMPLE FICTIF ILLUSTRATIF]" en préfixe de chaque ligne.** | règle-globale | CLAUDE.md (règle n°2 — renforcement), .claude/agents/orchestrator.md | fait | propagé |

## Session 10 — 2026-03-31

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S10 | 2026-03-31 | insistance | P0 | [INSISTANCE FONDATEUR] Ne JAMAIS faire le travail d'un agent spécialisé à sa place. Si un agent est bloqué (permissions, timeout, erreur), le RELANCER avec les bonnes conditions — pas prendre le relais manuellement. Thomas l'a signalé quand l'orchestrateur a voulu écrire le fichier client-manager.md au lieu de relancer @agent-factory. | Relancé @agent-factory au lieu de créer le fichier manuellement | **RÈGLE ABSOLUE (renforcement règle n°4 CLAUDE.md) : si un agent est bloqué, le relancer avec les bonnes conditions. Ne JAMAIS produire un livrable à la place d'un agent spécialisé, même si "c'est plus rapide".** Les seules exceptions restent : éditions techniques mineures, réponses utilisateur, opérations git, modifications project-context.md/CLAUDE.md. | règle-globale | CLAUDE.md (règle n°4 déjà présente — renforcement), docs/lessons-learned.md | fait | n/a |
| S10 | 2026-03-31 | problème | P0 | [BUG TECHNIQUE] Les sous-agents (subagent_type) se voient REFUSER la permission Write au runtime même si elle est dans .claude/settings.json et dans le frontmatter tools de l'agent. Le système prompt l'utilisateur qui n'est pas là pour approuver → l'agent est bloqué. Reproduit 3 fois avec @agent-factory. | Contournement : relancer en agent general-purpose (pas de subagent_type) qui hérite de toutes les permissions. L'agent utilise Bash(cat heredoc) si Write est refusé. | **WORKAROUND : si un agent spécialisé (subagent_type) est bloqué par les permissions Write/Edit après 1 tentative, le relancer en agent general-purpose avec le prompt complet de l'agent spécialisé.** L'agent general-purpose a accès à tous les outils (*). Ne PAS perdre 3 tentatives — switcher dès le premier refus. | règle-globale | docs/lessons-learned.md | fait | n/a |
| S10 | 2026-03-31 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Arya n'est pas soumise. Elle prend du recul comme une vraie chef de projet — elle challenge, recommande, et escalade à @moi quand elle ne sait pas. Elle ne fait pas juste ce qu'on lui dit. | Ajouté dans client-manager.md : section escalade @moi + "Ce qu'Arya ne fait PAS" | Tout agent PM/opérationnel doit avoir un protocole d'escalade clair ET savoir dire non/challenger. Un agent docile produit des livrables médiocres. | agent-spécifique | .claude/agents/client-manager.md | fait | propagé |
| S10 | 2026-03-31 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut un back-office centré sur l'INBOX DE VALIDATION, pas sur un menu de 20 pages. Arya prépare, la PM valide. Le concept central est "inbox de validation, pas panneau de contrôle". | Inbox UI implémentée comme page principale /admin, sidebar réduite à 5 items | Le back-office doit être centré sur le WORKFLOW (que dois-je faire maintenant ?) pas sur les OUTILS (quel outil utiliser ?). Pattern Superhuman/Linear, pas WordPress. | documentation | docs/pm/arya-protocols.md | fait | propagé |
| S10 | 2026-03-31 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut que les emails soient des BROUILLONS Outlook, pas des envois directs. La PM a toujours le dernier mot. Arya prépare, la PM envoie. | createDraft() implémenté, route /api/admin/emails/draft au lieu de send | Toute action client-facing (email, devis, livraison) doit passer par une validation humaine. Arya ne clique jamais "Envoyer" elle-même. | agent-spécifique | .claude/agents/client-manager.md, docs/pm/arya-protocols.md | fait | propagé |
| S10 | 2026-03-31 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut que les connaissances d'Arya soient PERTINENTES et UTILISÉES. Chaque entrée dans la knowledge base doit passer le test "est-ce que cette info change la façon dont on traite le prochain projet ?" Et Arya doit activement charger et utiliser ces connaissances à chaque interaction. | relevanceCheck dans le schema POST, injection systématique dans les protocoles via buildClientKnowledgePrompt() | Les knowledge bases IA ne valent rien si elles ne sont pas (a) curatées (pertinence) et (b) injectées systématiquement (usage). Un journal passif est du bruit. | règle-globale | .claude/agents/client-manager.md, src/lib/arya/knowledge-loader.ts | fait | propagé |
| S10 | 2026-03-31 | pattern | P1 | Le pattern "audit par l'agent utilisateur" (Arya audite son propre back-office comme une PM) est extrêmement efficace. Il détecte des gaps que @qa, @design et @ux ratent car ils n'ont pas la perspective métier. | Arya audit 6.5→8.5/10, 3 gaps critiques identifiés et corrigés | Systématiser le pattern : après chaque livrable client-facing, le PERSONA audite (pas juste @qa/@reviewer). L'agent @client-manager pour le back-office, les testeurs-persona pour le site public. | règle-globale | CLAUDE.md (déjà couvert par les gates GP/GC), docs/pm/arya-protocols.md | fait | n/a |
| S10 | 2026-03-31 | performance-ia | P1 | Les knowledge loaders sans token cap risquent d'injecter 15K+ tokens par appel (50+ entries client + 20 team + 20 rules). @ia a estimé $135/mois en surcoût dans le pire cas. | Token budget caps ajoutés : 3K tokens client, 3K team, 1.5K rules. Truncation intelligente avec notice. | Tout loader de contexte dynamique (knowledge base, rules, history) DOIT avoir un plafond de tokens. Pas de prompt ouvert illimité. Le prompt caching Anthropic réduirait le coût de 90% en plus. | agent-spécifique | src/lib/arya/knowledge-loader.ts, src/lib/arya/team-knowledge-loader.ts, src/lib/arya/rules-loader.ts | fait | propagé |
| S10 | 2026-03-31 | problème | P1 | Les routes /api/admin/cron/* sont bloquées par le middleware auth cookie. Les crons externes appellent avec x-cron-secret, pas un cookie. Le middleware intercepte avant que le handler ne vérifie le secret → 401 systématique. | Ajouté un bypass dans middleware.ts : `if (pathname.startsWith("/api/admin/cron")) return NextResponse.next()` | Les routes cron/webhook DOIVENT être exemptées du middleware auth cookie. Leur authentification est gérée par leur propre mécanisme (secret header, clientState). | agent-spécifique | src/middleware.ts | fait | propagé |

## Session 9 — 2026-03-28

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S9 | 2026-03-28 | préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] Tout lien SharePoint dans le back-office (affichage, stockage, partage) DOIT être un lien anonyme "Anyone" — jamais une URL directe navigateur qui nécessite auth. | Ajout getPublicSharingLink() dans sharepoint.ts + API endpoint share-link + bouton Folder convertit on-demand | **RÈGLE : Liens SharePoint "Anyone" obligatoires.** Tout lien SP affiché, stocké ou partagé dans le back-office doit être converti en lien anonyme via Graph API createLink scope:anonymous. Ne JAMAIS afficher/stocker les URLs directes du navigateur. | règle-globale | project-context.md, .claude/agents/fullstack.md, .claude/agents/infrastructure.md | fait | propagé |
| S9 | 2026-03-28 | insistance | P0 | Thomas a demandé 5+ fois que le Purpose of Work des quotes soit 2 vraies phrases montrant la compréhension du projet — pas juste le titre répété ou un tiret. | Réécriture buildPurpose() : utilise le brief ClickUp quand disponible, fallback intelligent category/type. Toujours 2 phrases : compréhension + livrables. | Le Purpose of Work d'un devis est un texte commercial qui montre la compréhension du projet client. Il doit utiliser le brief ClickUp quand disponible. Jamais juste répéter le nom du projet. | agent-spécifique | .claude/agents/fullstack.md | fait | propagé |
| S9 | 2026-03-28 | insistance | P0 | [INSISTANCE FONDATEUR x4+] Quand Thomas demande un changement, il doit être RÉELLEMENT appliqué et vérifié avant de dire "fait". Ne jamais dire "fait" sans avoir vérifié le fichier déployé. Ne jamais déléguer à un agent sans vérifier son output. Ne jamais être paresseux — viser l'excellence sur chaque point. Si un changement est demandé 2+ fois, c'est un échec de process. | Enregistré comme règle P0 permanente | **RÈGLE ABSOLUE : vérifier chaque changement après application.** Grep le fichier pour confirmer que le changement est réel. Ne JAMAIS dire "fait" sur la base du rapport d'un agent sans vérification. Si Thomas demande quelque chose 2+ fois, c'est un bug de process à corriger immédiatement. | règle-globale | CLAUDE.md, docs/lessons-learned.md | fait | propagé |
| S9 | 2026-03-28 | préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] Ton des réponses client Sarani : dynamiques, pro et bons, toujours cool et toujours dispo. Miroir de la langue et du registre du client. Prénom obligatoire. Jamais corporate lourd. Structure : confirmation + compréhension + questions naturelles + délai + signature. Draft validé avant envoi. | Enregistré dans founder-preferences.md, à intégrer dans le prompt de génération de réponse auto-brief. | **RÈGLE : Registre Sarani pour les réponses client.** Ton = dynamique, pro, cool, dispo. Langue = miroir du mail reçu. Registre = miroir (casual/formel) mais toujours Sarani. Prénom du client. Jamais de corporate boilerplate. | règle-globale | docs/founder-preferences.md, docs/product/auto-brief-specs.md, src/lib/ai/prompts/ | fait | propagé |

## Session 8 — 2026-03-28

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Cible propagation | Fichiers impactés | Statut correction | Statut propagation |
|---|---|---|---|---|---|---|---|---|---|---|
| S8 | 2026-03-28 | préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] Chaque page doit suivre le principe "conviction-first, not conversion-first" — le hero de chaque page doit correspondre à l'INTENTION du visiteur, pas plaquer un pitch commercial générique. Les CTAs ne doivent apparaître qu'après que le visiteur ait eu les raisons d'agir. | About restructurée (10→8 sections, 0 CTA en hero), Services H1 reformulé + CTA supprimé du hero, Blog footer CTA commercial → "Explore more" soft | **RÈGLE : Audit "intention-first" obligatoire sur chaque page.** Avant de produire/auditer une page, identifier l'intention du visiteur (découverte/évaluation/portfolio/qualification/action/éducation) et vérifier que le hero, les CTAs et la structure servent cette intention — pas une intention générique de conversion. Les CTAs commerciaux ("Start a project") ne doivent apparaître que sur les pages dont l'intention est ACTION (homepage hero, contact) ou en conclusion d'une page de conviction (About closing, Pricing closing). Sur les pages d'évaluation (Services, Work), d'éducation (Blog) ou de conviction (About), les CTAs sont déplacés en fin de parcours. | règle-globale | CLAUDE.md (règle commune agents), .claude/agents/ux.md, .claude/agents/copywriter.md, .claude/agents/design.md, .claude/agents/reviewer.md (gate ad-hoc) | fait | propagé |
| S8 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Les H1 de pages différentes ne doivent PAS être interchangeables — chaque page doit avoir un H1 unique qui ancre sur son intention spécifique. Services avait le même H1 que la Homepage ("Enterprise creative. Delivered in 24 hours.") | Services H1 reformulé : "8 disciplines. One team. No waiting." — ancré sur l'étendue des services, pas sur la vitesse (déjà acquise en homepage) | Chaque page doit avoir un H1 qui répond à la question "pourquoi suis-je sur CETTE page et pas une autre ?" Si le H1 pourrait être sur une autre page du site, il est trop générique. | règle-globale | .claude/agents/copywriter.md, .claude/agents/seo.md | fait | propagé |
| S8 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] CTA texte blanc sur Flame, PAS noir — Thomas confirme que le noir sur Flame ne ressort pas assez. Le blanc est WCAG AA pour le large text (bold 16px+ = 3.94:1 > 3:1 seuil). | text-brand-black → text-brand-white sur Button primary, ContactForm submit, StickyCTAMobile | Le texte des CTAs primaires Flame est BLANC. Ne pas revenir au noir même si un audit WCAG le recommande — Thomas a tranché explicitement. La conformité AA est assurée car tous les CTAs sont en large text (bold 16px+). | règle-globale | docs/design/design-system.md (section 3.2 Button), .claude/agents/design.md | fait | propagé |
| S8 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Hero homepage = dots animés (Flame/Cerulean/Lemon du logo Sarani), PAS de grille d'images. Thomas a vu le grid et l'a rejeté ("cata"). Les dots sont la signature visuelle créative de Sarani. | Grid supprimé, dots premium en 3 couches (primaire/secondaire/ambient avec blur bokeh) | Le hero homepage utilise UNIQUEMENT les dots animés du logo Sarani côté droit. Ne JAMAIS remplacer par une grille d'images, un carrousel, ou un visuel statique. Les dots sont la signature créative. | agent-spécifique | .claude/agents/design.md, .claude/agents/fullstack.md | fait | propagé |

## Session 7 — 2026-03-28

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| S7 | 2026-03-28 | problème | P0 | Drizzle migrator ne fonctionnait pas avec des entrées journal ajoutées manuellement — migrations 0002-0006 jamais exécutées | Seed script refait avec raw SQL (ensureSchema() avec CREATE TABLE IF NOT EXISTS + DO blocks) | Ne jamais dépendre du Drizzle migrator pour des migrations ajoutées manuellement. Utiliser raw SQL idempotent dans le seed comme filet de sécurité. | appliqué |
| S7 | 2026-03-28 | problème | P0 | Email 400 Bad Request — double /v1.0/v1.0/ dans les URLs Graph API | Supprimé le préfixe /v1.0/ dans email.ts — graphFetch l'ajoute déjà via GRAPH_BASE_URL | Quand une fonction wrapper (graphFetch) ajoute un préfixe, ne JAMAIS le re-ajouter dans les appels. Grep systématiquement le base URL dans tous les fichiers qui utilisent le wrapper. | appliqué |
| S7 | 2026-03-28 | problème | P0 | Page publique /lp/[slug] révélait le titre et le statut des landing pages non publiées | Remplacé par notFound() pour ne rien révéler | Toute page publique DOIT retourner 404 pour les ressources non publiées — jamais révéler l'existence ou le contenu d'un draft. | appliqué |
| S7 | 2026-03-28 | insistance | P0 | Thomas a demandé 3+ fois la validation 10/10 du PDF de devis — "est-ce pro, est-ce beau, est-ce Sarani, même identité que le site, propre, aligné, aéré" | Audit @design approfondi avec 7 critères explicites + 15 corrections précises appliquées | Pour les livrables client-facing (PDF, emails, proposals), lancer un audit @design systématique avec les 7 critères de Thomas avant de présenter le résultat. Ne JAMAIS livrer un PDF sans audit design. | appliqué |
| S7 | 2026-03-28 | insistance | P1 | Thomas a demandé 2+ fois que les montants dans le tableau de pricing soient alignés à droite | Right-align via widthOfTextAtSize() sur toutes les colonnes monétaires | Les colonnes numériques dans les tableaux (PDF, UI) DOIVENT toujours être right-aligned. C'est un standard comptable universel. | appliqué |
| S7 | 2026-03-28 | requête | P1 | Thomas veut les proposals sous forme de lien web magnifique (pas PDF) avec données Sarani pré-remplies | Template "Proposal" ajouté dans les specs landing-page-generator avec auto-fill case studies + conditions Sarani | Les documents commerciaux client-facing doivent migrer du PDF vers le web (liens partageables). Le PDF reste pour les devis/factures uniquement. | ouvert |
| S7 | 2026-03-28 | requête | P1 | Thomas veut que les case studies soient générées automatiquement à partir du tracker sans intervention humaine | Scoring engine 6 critères + auto-scan + auto-generation au-dessus du seuil 70/100 | L'automatisation par défaut est le standard Thomas — le fondateur ne veut pas de tâches manuelles récurrentes. Tout workflow récurrent doit être automatisé. | ouvert |
| S7 | 2026-03-28 | biais | P1 | Le reviewer audit a signalé G7 FAIL sur le prompt library (contradiction provider PiAPI vs multi-model) alors que Thomas avait validé le changement plus tôt dans la session | Identifié comme faux positif — video-ai-specs.md avait été mis à jour avec H-03 | Le reviewer DOIT lire les mises à jour récentes des livrables amont, pas seulement la version originale. Ajouter un check "date de dernière modification" dans le protocole reviewer. | ouvert |
| S7 | 2026-03-28 | pattern | P1 | La boucle audit→correction→revalidation a bien fonctionné : chaque livrable a atteint 10/10 en 1-2 itérations max | — | Systématiser la boucle : @reviewer/@design audit → corrections @fullstack/@product-manager → re-run audit si < 9/10. Max 3 passes. | ouvert |
| S7 | 2026-03-28 | pattern | P2 | Le pattern "foundation first" (DB + API + UI mock, sans LLM) permet de valider l'architecture avant l'intégration coûteuse | — | Pour toute feature IA, implémenter la fondation (DB/API/UI) en session 1, l'intégration LLM en session 2. Cela permet de valider le flux complet avec des mocks avant d'investir dans les prompts/API IA. | ouvert |
| S7 | 2026-03-28 | préférence fondateur | P0 | [PRÉFÉRENCE FONDATEUR] Thomas exige 10/10 sur TOUS les livrables — pas de "bon enough". Il veut que @creative-strategy, @design, @qa valident chaque livrable avant présentation | — | Le seuil de qualité Thomas est 10/10, pas 8/10. Ne JAMAIS présenter un livrable sans audit multi-agents. | ouvert |
| S7 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut que les proposals soient des liens web, pas des PDF — "Option 1 = lien web magnifique" | — | Le web > le PDF pour tout ce qui est client-facing et interactif. Le PDF reste pour les documents contractuels (devis, factures). | ouvert |
| S7 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut l'automatisation maximale — "que les case studies soient générés tout seul" sans intervention | — | Thomas pense en systèmes autonomes, pas en workflows manuels. Chaque feature doit être conçue pour fonctionner sans intervention humaine par défaut. | ouvert |
| S7 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas juge la qualité sur 7 critères visuels : PRO, BEAU, SARANI, MÊME SITE, PROPRE, ALIGNÉ, AÉRÉ | — | Grille de validation visuelle Thomas : 7 critères, chacun doit être 10/10. Utiliser cette grille pour tout livrable visuel (PDF, page, composant). | ouvert |
| S7 | 2026-03-28 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut que le storyboard soit une étape intermédiaire AVANT la vidéo — validation progressive | — | Thomas préfère les flux progressifs (brief → storyboard → vidéo) aux flux directs (brief → vidéo). Chaque étape permet un point de validation client. | ouvert |
| S7 | 2026-03-28 | performance-ia | P2 | Les agents @product-manager ont parfois timeout sur des specs longues (>500 lignes). La technique "écriture en 3 appels" (Write + Edit + Edit) a résolu le problème. | — | Pour les livrables > 150 lignes, imposer l'écriture en blocs de < 100 lignes via des appels Edit séquentiels. Déjà dans CLAUDE.md mais pas toujours respecté par les agents. | ouvert |

## Session 6 — 2026-03-27

| Session | Date | Catégorie | Sévérité | Description | Correction appliquée | Recommandation framework | Statut |
|---|---|---|---|---|---|---|---|
| S6 | 2026-03-27 | problème | P0 | URLSearchParams encode $ en %24 — Graph API rejette les OData params ($filter, $orderby) | Remplacé par construction manuelle de la query string | Tout appel Microsoft Graph avec OData params DOIT utiliser une string manuelle, PAS URLSearchParams | appliqué |
| S6 | 2026-03-27 | problème | P0 | pdf-lib ne supporte pas woff2 (même avec fontkit) — crash silencieux au runtime | Fallback sur Helvetica StandardFonts | Pour utiliser des custom fonts dans pdf-lib, fournir des fichiers .ttf — pas de woff2 ni woff | appliqué |
| S6 | 2026-03-27 | insistance | P0 | Thomas a demandé 3+ fois que le Purpose of Work des devis ne copie pas les briefs/URLs bruts | Garde-fou buildPurpose() qui strip toutes les URLs et construit une phrase propre | Tout champ "description" client-facing DOIT avoir un sanitizer qui rejette les URLs, les briefs complets, et les métadonnées internes | appliqué |
| S6 | 2026-03-27 | insistance | P1 | Thomas a signalé 2 fois les liens Folder pointant vers replit.app au lieu de SharePoint | Filtre URL dans excel-parser.ts — ne garde que sharepoint.com/onedrive.com/1drv.ms | Les données Excel peuvent contenir des URLs parasites — toujours filtrer côté parsing, pas côté UI | appliqué |
| S6 | 2026-03-27 | insistance | P1 | Thomas a trouvé les icônes d'action du tracker incompréhensibles (3+ demandes de clarification) | Remplacé par boutons texte (Quote/Excel/ClickUp/Files) | Pour les actions dans les tables, préférer les labels texte aux icônes seules — surtout dans un back-office opérationnel | appliqué |
| S6 | 2026-03-27 | requête | P1 | Thomas veut que les devis soient en FR ou EN selon le client | Système de traduction complet avec TRANSLATIONS object (EN/FR) + sélecteur dans le formulaire | Tout document client-facing DOIT supporter au minimum EN/FR | appliqué |
| S6 | 2026-03-27 | requête | P1 | Thomas veut les payment terms configurables par client | Champ paymentTermsDays ajouté au schema DB + input dans le formulaire de devis | Les conditions commerciales (délais, TVA) doivent être par client, pas globales | appliqué |
| S6 | 2026-03-27 | biais | P1 | L'agent @fullstack a utilisé AnimatePresence pour le menu mobile — causait un délai de montage sur mobile | Remplacé par CSS opacity/visibility (toujours dans le DOM) | Pour les overlays mobiles, préférer CSS toggle (instantané) à framer-motion mount/unmount (1-2 frames de délai) | appliqué |
| S6 | 2026-03-27 | pattern | P2 | L'audit croisé @design × @ux × @pm sur la sidebar a produit des recommandations convergentes et non-contradictoires — 3 agents, 1 round, résultat propre | — | Pour les décisions UI structurantes, lancer 3 audits parallèles (@design, @ux, @pm) puis croiser — plus efficace qu'un seul audit séquentiel | ouvert |
| S6 | 2026-03-27 | pattern | P2 | Le stale-while-revalidate localStorage accélère drastiquement le chargement perçu du tracker | — | Pour toute page qui fetch des données lentes (>3s), implémenter un cache localStorage avec affichage instantané + refresh en background | ouvert |
| S6 | 2026-03-27 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas ne veut JAMAIS être mentionné par son nom sur le site public — "the founding team" uniquement | Remplacé dans about/page.tsx | Règle absolue : zéro nom individuel sur le site public Sarani | appliqué |
| S6 | 2026-03-27 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas juge les devis "simplistes" pour une agence de designers — le PDF doit refléter le niveau créatif de l'agence | Refonte PDF premium avec branding Flame, bloc client, accents | Les documents client-facing (devis, proposals) doivent être au même niveau que le site web en termes de design | appliqué |
| S6 | 2026-03-27 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas veut que les réponses email aux clients reflètent le ton du client ("on les connait et on les aime bien") — pas de corporate boilerplate | Prompt tone mirroring dans le reply generator | La communication client est informelle et chaleureuse — matcher le registre du client, utiliser le prénom | appliqué |
| S6 | 2026-03-27 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas utilise un format de brief spécifique avec emojis (🌟 Introduction, ✈️ Brief, 🚚 Deliverables, 📍 Source Files, 💬 Branding, ➡️ Others) | Templates mis à jour dans src/lib/brief-templates.ts | Les templates doivent correspondre aux outils réels de l'équipe, pas à un format théorique | appliqué |
| S6 | 2026-03-27 | préférence fondateur | P2 | [PRÉFÉRENCE FONDATEUR] Thomas préfère left-aligned pour les pages web (choix explicite quand on lui a demandé "left ou centré ?") | Toutes les pages secondaires en left-aligned | Left-aligned = positionnement premium agence. Centré = SaaS/product. Sarani est une agence. | appliqué |
| S6 | 2026-03-27 | performance-ia | P2 | Claude Haiku pour le brief check est rapide (<2s) et suffisant pour des classifications — pas besoin de Sonnet/Opus pour cette tâche | — | Utiliser Haiku pour les tâches de classification/validation simples — Sonnet/Opus réservé au contenu créatif et à la stratégie | ouvert |
| S6 | 2026-03-27 | requête | P1 | Thomas veut que le prompt engineering soit fait AVANT l'implémentation de la vidéo IA — "le meilleur prompt du monde possible" | Note ajoutée dans video-ai-specs.md — @pm + @ia doivent collaborer sur le prompt library avant que @fullstack code | Pour toute feature dépendant d'un prompt IA (génération vidéo, image, texte), le prompt engineering est un livrable à part entière — pas un détail d'implémentation. @ia doit le produire et le tester avant le code. | ouvert |
| S6 | 2026-03-27 | préférence fondateur | P1 | [PRÉFÉRENCE FONDATEUR] Thomas considère le prompt engineering comme critique pour la vidéo IA — veut le "meilleur prompt du monde possible" avant toute implémentation | — | Thomas traite le prompt comme un actif stratégique, pas un paramètre technique. Le prompt library doit être itéré et testé avant le code. | ouvert |
